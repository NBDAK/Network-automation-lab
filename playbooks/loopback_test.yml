- name: Configure Loopback Interfaces
  hosts: routers
  gather_facts: no
  tasks:
    - name: Create Loopback with IP
      ios_config:
        lines:
          - ip address {{ item.ip }} {{ item.mask }}
        parents:
          - interface Loopback{{ item.id }}
      loop: "{{ loopbacks }}"
